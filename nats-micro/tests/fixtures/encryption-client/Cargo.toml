[package]
name = "encryption-client"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
nats-micro = { path = "../../..", default-features = false, features = ["client", "encryption"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
